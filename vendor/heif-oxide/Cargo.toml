# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
name = "heif-oxide"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"
description = "Pure-Rust HEIF/HEIC still-image decoder (ISOBMFF container + HEVC via rust_h265). Decodes iPhone photos — grid tiles, 10-bit, Display P3 — with no C dependencies."
readme = "README.md"
repository = "https://github.com/dan335/heif-oxide"
license = "MIT OR Apache-2.0"
keywords = [
  "decoder",
  "heic",
  "heif",
  "hevc",
  "image",
]
categories = [
  "multimedia::encoding",
  "multimedia::images",
]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "heif_oxide"
path = "src/lib.rs"

[dependencies.rust_h265]
version = "0.1"

[dependencies.thiserror]
version = "2"
