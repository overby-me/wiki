{
  "lexicon": 1,
  "id": "com.example.wiki.comment",
  "defs": {
    "main": {
      "type": "record",
      "description": "A public comment on a public content item (a resolution, public document, or another comment). Internal discussion on private items stays in the org store and is never a record.",
      "key": "tid",
      "record": {
        "type": "object",
        "required": ["subject", "text", "createdAt"],
        "properties": {
          "subject": {
            "type": "ref",
            "ref": "com.atproto.repo.strongRef",
            "description": "The public record this comments on."
          },
          "text": {
            "type": "string",
            "maxLength": 10000,
            "maxGraphemes": 1000
          },
          "parent": {
            "type": "ref",
            "ref": "com.atproto.repo.strongRef",
            "description": "Optional: the comment this is a reply to (threading)."
          },
          "createdAt": {
            "type": "string",
            "format": "datetime"
          }
        }
      }
    }
  }
}
